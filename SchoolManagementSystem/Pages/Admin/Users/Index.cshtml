@page
@model SchoolManagementSystem.Pages.Admin.Users.IndexModel
@{
    ViewData["Title"] = "إدارة المستخدمين";
    Layout = "/Pages/Shared/_Layout.cshtml"; // Adjust if you have an Admin Layout
}

<div class="container-fluid px-4">
    <h1 class="mt-4">إدارة المستخدمين</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a href="/">لوحة التحكم</a></li>
        <li class="breadcrumb-item active">المستخدمين</li>
    </ol>

    <div class="card mb-4">
        <div class="card-header">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <i class="fas fa-table me-1"></i>
                    جميع المستخدمين
                </div>
                <a asp-page="Create" class="btn btn-primary btn-sm">إضافة مستخدم جديد</a>
            </div>
        </div>
        <div class="card-body">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>اسم المستخدم / البريد الإلكتروني</th>
                        <th>الصلاحيات</th>
                        <th>العمليات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model.UsersWithRoles)
                    {
                        <tr>
                            <td>@user.Email</td>
                            <td>@string.Join(", ", user.Roles)</td>
                            <td>
                                <!-- Add Edit/Delete links if needed -->
                                <a asp-page="./Edit" asp-route-id="@user.Id" class="btn btn-sm btn-primary">
                                    <i class="fas fa-edit"></i> تعديل
                                </a>
                                @if (user.Id != Model.CurrentUserId)
                                {
                                    <form method="post" asp-page-handler="Delete" asp-route-id="@user.Id" class="d-inline ms-1" onsubmit="return confirm('هل أنت متأكد من حذف هذا المستخدم؟ لا يمكن التراجع عن هذه العملية.');">
                                        <button type="submit" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
                                    </form>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
